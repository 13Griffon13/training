import 'package:flutter/material.dart';

class OtherData extends StatelessWidget {
  const OtherData({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Other data'));
  }


}
